# Optional: if omitted, the server generates and stores a password in data-packages/.admin-password.
ADMIN_PASSWORD=replace-with-a-strong-password

# Optional server settings.
PORT=8787
HOST=0.0.0.0
MAX_UPLOAD_MB=2048
