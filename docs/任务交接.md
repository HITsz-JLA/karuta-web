# Karuta Web 任务交接

更新时间：2026-09-08

## 当前目标

把 `HITsz-JLA/karuta-web` 部署成一个可在校园网访问的歌牌 Web 应用：

- 歌牌数据包保存在服务器本地；普通用户只读。
- 管理员使用独立的 `/admin` 页面上传数据包。
- 适配 JLA-MUCA 新版 `meta/metadata.csv` 格式。
- 正式对局歌曲最多播放 30 秒。
- 数据包支持两种模式：
  - **完整包**：保留 full 和前 30 秒片段；对局播放片段，休息音乐播放 full。
  - **精简包**：只保留前 30 秒片段；对局和休息音乐都播放片段。

## 已完成

1. 已在本地克隆并安装项目：`C:\Users\hiral\Desktop\fsdownload\karuta-web`。
2. 已增加 Express 本地服务器：
   - API 默认监听 `0.0.0.0:8787`。
   - 前端开发服务器默认监听 `0.0.0.0:5173`。
   - 数据包目录为 `data-packages/`，ZIP 文件不会提交到 Git。
3. 已增加独立管理员页面：`/#/admin`。
4. 已实现管理员登录、服务器数据包列表、ZIP 上传；首页普通用户只能读取并加载数据包。
5. 已增加 ZIP 导入进度显示，并在导入失败时清理已经写入 IndexedDB 的临时音频和图片。
6. 已把当前对局歌曲和休息音乐的播放上限改为 30 秒。
7. 已为校园网访问放行本机 TCP 5173 端口（当前电脑上的防火墙规则）。

## 远程数据包调查结果

SSH 目标：`root@111.228.7.90:22`。密钥文件名为 `keygura.pem`，密钥本身不在仓库中。

数据包目录：`/root/HITSZ-JLA/JLA-MUCA/`

关键目录：

```text
meta/metadata.csv
mp3_files/full/<category>/...
mp3_files/seg_30/<category>/...
music_cover/<category>/...
music_cards/
```

已核对的 `metadata.csv`：

- 478 行歌曲记录。
- 436 个 `category + work_number` 作品组。
- 类别：BANGDREAM 78、PJSK 106、GALGAME 158、ANIME 136。
- 单个作品最多 3 首歌。
- 所有记录都能找到对应的 `mp3_files/seg_30` 音频。
- `audio_path` 是 `mp3_files/<category>/...`，导入前需要映射到 `mp3_files/seg_30/<category>/...` 或 `mp3_files/full/<category>/...`。

## 尚未完成的核心工作

这部分是下一次继续时的优先事项：

1. 在 `src/lib/zipPackage.ts` / `src/lib/csv.ts` 增加新版 metadata 解析：
   - 优先选择 `meta/metadata.csv`，不能随便取 ZIP 中第一个 CSV。
   - 按 `category + work_number` 合并多首歌曲。
   - 使用 `cover_paths` / `cover_files` 找图片。
   - 使用 `mp3_files/seg_30` 作为对局片段，并在完整包中同时保存 `mp3_files/full`。
2. 在 `SongEntry` 中增加可选的 full 音频引用，例如 `fullBlobKey`：
   - `blobKey` 保持为对局用的 30 秒片段。
   - `fullBlobKey` 仅完整包存在。
   - 休息音乐有 `fullBlobKey` 时播放完整音频且不加 30 秒定时器。
3. 增加完整包 / 精简包选择：
   - 管理员上传数据包时选择模式，并随服务器数据包元数据保存。
   - 编辑器导出 ZIP 时也提供同名选项；导入器可读取包内 manifest 或服务器模式。
4. 用远程 JLA-MUCA 数据包做实际导入测试，确认 436 张作品、478 首歌、图片和多歌曲作品都正确。

## 换设备继续的启动方式

在新设备克隆仓库后：

```powershell
npm ci
Copy-Item .env.example .env
# 编辑 .env，设置一个新的 ADMIN_PASSWORD
npm run build
```

开发测试需要开两个终端：

```powershell
# 终端 1：API 和服务器数据包服务
npm run server

# 终端 2：Vite 开发页面
npm run dev -- --host 0.0.0.0
```

浏览器访问：

- 本机：`http://127.0.0.1:5173/`
- 局域网：`http://<运行电脑的局域网 IP>:5173/`
- 管理员：`http://<运行电脑的局域网 IP>:5173/#/admin`

生产模式可使用：

```powershell
npm run build
npm start
```

此时 Express 会在 `8787` 提供构建后的页面和 API。

## 注意事项

- 不要提交 `keygura.pem`、`.env`、`data-packages/.admin-password` 或任何 ZIP 数据包。
- 当前电脑的管理员自动生成密码保存在被忽略的 `data-packages/.admin-password`；换设备后应在新设备重新设置 `ADMIN_PASSWORD`。
- 若要在远程服务器部署，需要另外配置 Node.js、项目目录、systemd/进程守护和远程防火墙；当前改动尚未推送到远程服务器运行环境。
